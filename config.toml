
# ----------------------------------
# 1. CORE BUILD SETTINGS
# ----------------------------------

base_url = "https://praveenrajsrivastav.github.io/portfolio"

compile_sass = false        # using plain CSS now, no SASS needed
build_search_index = false

# ----------------------------------
# 2. SITE IDENTITY
# ----------------------------------

title = "Praveen Raj Srivastav"
description = "3rd Year @ NMIT | Backend & Systems"

# hermit is kept so its asset pipeline still works,
# but every page template is overridden by our /templates/ folder.
theme = "hermit"

taxonomies = []             # removed — tags not needed for a portfolio

# ----------------------------------
# 3. EXTRA SITE METADATA
# ----------------------------------

[extra]
favicon = "me.png"
author  = "Praveen Raj Srivastav"

# hermit_menu and hermit_social intentionally removed —
# navigation is handled by the floating bottom nav in base.html,
# social links are hardcoded in the templates.

# ----------------------------------
# 4. THEME COLOR HINTS
# (used if any hermit partial bleeds through on edge cases)
# ----------------------------------

[extra.style]
accent     = "#a277ff"
background = "#0f0f0f"
text       = "#e2e2e2"
